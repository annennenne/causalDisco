#' @param version Character. The version of Tetrad to install.
#'   Default is "`r getOption('causalDisco.tetrad.version')`".
#'   Use `options(causalDisco.tetrad.version = "...")` to change the default version.
