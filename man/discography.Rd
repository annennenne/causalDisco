% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discography.R
\name{discography}
\alias{discography}
\title{discography — a tidy tibble for causal graphs}
\usage{
discography(x, nodes = NULL, ...)
}
\arguments{
\item{x}{A causal-graph object (see methods below).}

\item{nodes}{Optional character vector giving the desired node order.
If \code{NULL}, the unique node names are sorted alphabetically.}

\item{...}{Passed on to methods.}
}
\value{
A tibble with class \code{"discography"}.
}
\description{
A lightweight S3 generic that converts many causal-discovery outputs into a
tibble with three columns:
\itemize{
\item \strong{from} — tail-end node
\item \strong{to}   — head-end node (or the other endpoint for symmetric edges)
\item \strong{edge_type} — one of \code{"-->"}, \code{"---"}, \code{"<->"}, \code{"o-o"}, \code{"--o"}, \code{"o->"}
}

Symmetric relations (\code{"---"}, \code{"<->"}, \code{"o-o"}) appear \strong{once},
ordered by the supplied \code{nodes} vector (or alphabetically if \code{nodes} is \code{NULL}).
Self-loops, if present, are kept.
}
