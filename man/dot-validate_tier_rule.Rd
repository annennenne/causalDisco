% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knowledge.R
\name{.validate_tier_rule}
\alias{.validate_tier_rule}
\title{Validate that no edge runs from higher tier to lower tier}
\usage{
.validate_tier_rule(edges_df, tiers)
}
\arguments{
\item{edges_df}{A data frame with columns \code{status}, \code{from},
\code{to}, \code{tier_from}, and \code{tier_to}.}

\item{tiers}{A data frame with a column \code{label} listing tier labels}
}
\description{
Validate that no edge runs from higher tier to lower tier
}
\examples{
### .validate_tier_rule() example ###

# success case: edge goes from child to youth (allowed)
tiers <- tibble::tibble(label = c("child", "youth"))
edges <- tibble::tibble(
  status    = "required",
  from      = "child_x1",
  to        = "youth_x3",
  tier_from = "child",
  tier_to   = "youth"
)
causalDisco:::.validate_tier_rule(edges, tiers)

# violation case: required edge from youth to child (should error)
edges_bad <- tibble::tibble(
  status    = "required",
  from      = "youth_x3",
  to        = "child_x1",
  tier_from = "youth",
  tier_to   = "child"
)
try(causalDisco:::.validate_tier_rule(edges_bad, tiers))
}
\keyword{internal}
