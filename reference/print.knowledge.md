# Print a `knowledge` object

Print a `knowledge` object

## Usage

``` r
# S3 method for class 'knowledge'
print(x, ...)
```

## Arguments

- x:

  A `knowledge` object.

- ...:

  Additional arguments (not used).
